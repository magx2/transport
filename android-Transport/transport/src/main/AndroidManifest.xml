<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.grzeslowski.transport" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:logo="@drawable/ic_action_ic_launcher_bus"
        android:label="@string/app_name"
        android:theme="@style/Theme.Transporter" >
        <activity
            android:name="pl.grzeslowski.transport.activities.MainActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pl.grzeslowski.transport.activities.ResultActivity_"
            android:label="@string/title_activity_result" >
        </activity>

        <!-- Crashlitics -->
        <meta-data android:name="com.crashlytics.ApiKey" android:value="bd9630002e4eab16b6f7b10d695b495bda9ceb93"/>

    </application>

</manifest>
